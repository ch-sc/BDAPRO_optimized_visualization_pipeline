{
  "$schema": "https://vega.github.io/schema/vega-lite/v3.json",
  "data": {"url": "data/GermanCreditAge25.csv"},
  "transform": [{
    "filter": {"and": [
      {"field": "DurationMonth", "valid": true},
      {"field": "CreditAmount", "valid": true}
    ]}
  }],
  "mark": "rect",
  "width": 300,
  "height": 200,
  "encoding": {
    "x": {
      "bin": {"maxbins":60},
      "field": "DurationMonth",
      "type": "quantitative"
    },
    "y": {
      "bin": {"maxbins": 40},
      "field": "CreditAmount",
      "type": "quantitative"
    },
    "color": {
      "aggregate": "count",
      "type": "quantitative"
    }
  },
  "config": {
    "range": {
      "heatmap": {
        "scheme": "greenblue"
      }
    },
    "view": {
      "stroke": "transparent"
    }
  }
}

